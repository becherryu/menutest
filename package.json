{
  "name": "menutest",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "backend": "cd backend && npm start && nodemon backend",
    "frontend": "cd frontend && npm start",
    "start": "concurrently --kill-others-on-fail \"npm run backend\" \"npm run frontend\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.0",
    "nodemon": "^3.0.1"
  }
}
